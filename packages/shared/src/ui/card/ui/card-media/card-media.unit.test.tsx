import { describe, it, expect, vi } from "vitest";
import { CardMedia } from "./card-media";
import { render, screen } from "@testing-library/react";

describe("CardMedia", () => {
    it("should render CardImage for type 'image' with correct props", () => {

    });

    it("should render div for type 'video' with correct classes", () => {

    });

    it("should render div for type 'audio' with correct classes", () => {

    });

    it("should apply correct classes from styles and custom className", () => {

    });

    it("should forward ref correctly", () => {
        
    });
});
